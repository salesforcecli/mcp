{
  "servers": {
    "Salesforce DX": {
      "command": "node",
      "args": [
        "${workspaceFolder}/packages/mcp/bin/run.js",
        "--toolsets",
        "orgs",
        "--tools",
        "get_foo,set_bar",
        "--orgs",
        "DEFAULT_TARGET_ORG",
        "--no-telemetry",
        "--allow-non-ga-tools"
      ]
    }
  }
}
